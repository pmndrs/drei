{
  "_comment": "The ONLY hand-maintained component data. Everything else in component-status.json is derived from the filesystem by scripts/audit-components.js. A `wont-port` entry MUST carry a reason — it is surfaced in the migration guide and should be surfaced in a runtime error too. `node scripts/audit-components.js --check` fails if an entry names a component that no longer exists.",
  "Effects": {
    "classification": "wont-port",
    "reason": "Wraps three-stdlib's EffectComposer, which is WebGL-only. WebGPU postprocessing goes through three's own RenderPipeline (useRenderPipeline in r3f v10), which is a different architecture rather than a port of this one."
  },
  "AsciiRenderer": {
    "classification": "wont-port",
    "reason": "Wraps three-stdlib's AsciiEffect, a WebGL-only DOM overlay effect with no WebGPU equivalent upstream."
  },
  "shaderMaterial": {
    "classification": "wont-port",
    "reason": "A factory for GLSL ShaderMaterials. TSL's equivalent is node-graph construction, a fundamentally different authoring model — WebGPU users write TSL directly rather than calling a GLSL factory."
  },
  "useBoxProjectedEnv": {
    "classification": "wont-port",
    "reason": "Implemented by patching a built-in material's GLSL through onBeforeCompile. TSL has no onBeforeCompile equivalent; box-projected environment mapping on WebGPU would be a new node-based implementation, not a port."
  },
  "ShadowAlpha": {
    "classification": "wont-port",
    "reason": "Patches the depth material's GLSL through onBeforeCompile to carry alpha into shadows. Same constraint as useBoxProjectedEnv — needs a new TSL implementation rather than a port."
  },
  "Text": {
    "classification": "todo",
    "reason": "Depended on a vendored troika fork that could not be published. Returns via @pmndrs/glyph, which covers both renderers. Tracked in #2658."
  },
  "PointMaterial": {
    "classification": "todo",
    "assignee": "human-only",
    "reason": "Lives in core/ and is exported from the root entry, but patches PointsMaterial fragment GLSL through onBeforeCompile, which NodeMaterial never calls. On WebGPU the points render as hard squares instead of antialiased circles — silently, with no error. Also reads renderer.capabilities, which three/webgpu Renderer does not have. Needs a TSL implementation. Tracked in #2818."
  },
  "Outlines": {
    "classification": "todo",
    "assignee": "human-only",
    "reason": "Broken on WebGPU through both entries. The root entry exports experimental/Effects/Outlines, a GLSL ShaderMaterial that cannot compile there; the /webgpu entry exports the TSL version, which throws on every construction because an accessor fires during super() (#2813). Tracked in #2818 and #2813."
  },
  "Splat": {
    "classification": "todo",
    "assignee": "human-only",
    "reason": "Root-exported from external/ and classified agnostic, but it is a raw GLSL ShaderMaterial and types THREE.WebGLRenderer into its public surface. The upstream technique is WebGL-only, so this is probably wont-port — but that needs deciding and writing down, plus a runtime error, rather than being left misclassified. Tracked in #2818."
  },
  "Preload": {
    "classification": "todo",
    "assignee": "agent-ok",
    "reason": "Calls gl.compile(), which on the WebGPU renderer is a getter aliasing compileAsync — it returns a promise nobody awaits, so preloading never happens. Not detectable by the agnostic check, because compile exists on both renderers and only the semantics differ. Tracked in #2809 and #2818."
  }
}
