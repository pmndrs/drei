{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    /* Match the root tsconfig. drei's source is pulled in through the `../src`
       path aliases so that the examples app stays a live testbed — you edit
       drei source and see it here immediately. That also means drei's files are
       checked here, so any rule stricter than root's (`noImplicitAny`,
       `noUnusedLocals`, ...) reports errors in files root considers fine.
       Keep these aligned with root, and run the same compiler: `typecheck` uses
       drei's own `typescript` (a newer tsc judged a drei `@ts-expect-error`
       stale that root's still needs). See #2808. */
    "noImplicitAny": false,
    "noImplicitThis": false,
    "noFallthroughCasesInSwitch": true,

    /* Path aliases */
    "baseUrl": ".",
    "paths": {
      "@ex/*": ["./src/*"],
      "@react-three/drei/core": ["../src/core"],
      "@react-three/drei/legacy": ["../src/legacy"],
      "@react-three/drei/webgpu": ["../src/webgpu"],
      "@react-three/drei/external": ["../src/external"],
      "@react-three/drei/experimental": ["../src/experimental"],
      "@react-three/drei": ["../src"],
      "three/webgpu": ["../node_modules/@types/three/build/three.webgpu.d.ts"],
      "three/tsl": ["../node_modules/@types/three/build/three.tsl.d.ts"],
      "#three": ["../node_modules/@types/three"],
      "#three-addons": ["../src/utils/three-addons"],
      "#drei-platform": ["../src/utils/drei-platform"],
      "@core/*": ["../src/core/*"],
      "@legacy/*": ["../src/legacy/*"],
      "@webgpu/*": ["../src/webgpu/*"],
      "@external/*": ["../src/external/*"],
      "@experimental/*": ["../src/experimental/*"],
      "@utils/*": ["../src/utils/*"],
      "@lib/*": ["../lib/*"]
    }
  },
  /* Only this app. drei's own source is typechecked by the root tsconfig, which
     has the correct #three -> @types/three mapping and drei's compiler options.
     Re-checking it here under different, stricter settings produced ~900 errors
     that were entirely configuration noise. See #2808. */
  "include": ["src"]
}
