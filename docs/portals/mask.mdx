---
title: useMask
sourcecode: src/core/Portal/Mask/Mask.tsx
---

[![](https://img.shields.io/badge/-storybook-%23ff69b4)](https://drei.pmnd.rs/?path=/story/portals-usemask--usemask-st)

Returns stencil properties to apply to a material for masking.

### Define mask and apply to mesh

```jsx
<Mask id={1}>
  <planeGeometry />
</Mask>

function MaskedMesh() {
  const stencil = useMask(1)
  return <mesh><meshStandardMaterial {...stencil} /></mesh>
}
```

### Compound mask (multiple shapes, same id)

```jsx
<Mask id={1} position={[-1, 0, 0]}><planeGeometry /></Mask>
<Mask id={1} position={[1, 0, 0]}><circleGeometry /></Mask>
```