{
  "_comment": "The ONLY hand-maintained component data. Everything else in component-status.json is derived from the filesystem by scripts/audit-components.js. A `wont-port` entry MUST carry a reason — it is surfaced in the migration guide and should be surfaced in a runtime error too. `node scripts/audit-components.js --check` fails if an entry names a component that no longer exists.",

  "Effects": {
    "classification": "wont-port",
    "reason": "Wraps three-stdlib's EffectComposer, which is WebGL-only. WebGPU postprocessing goes through three's own RenderPipeline (useRenderPipeline in r3f v10), which is a different architecture rather than a port of this one."
  },
  "AsciiRenderer": {
    "classification": "wont-port",
    "reason": "Wraps three-stdlib's AsciiEffect, a WebGL-only DOM overlay effect with no WebGPU equivalent upstream."
  },
  "shaderMaterial": {
    "classification": "wont-port",
    "reason": "A factory for GLSL ShaderMaterials. TSL's equivalent is node-graph construction, a fundamentally different authoring model — WebGPU users write TSL directly rather than calling a GLSL factory."
  },
  "useBoxProjectedEnv": {
    "classification": "wont-port",
    "reason": "Implemented by patching a built-in material's GLSL through onBeforeCompile. TSL has no onBeforeCompile equivalent; box-projected environment mapping on WebGPU would be a new node-based implementation, not a port."
  },
  "ShadowAlpha": {
    "classification": "wont-port",
    "reason": "Patches the depth material's GLSL through onBeforeCompile to carry alpha into shadows. Same constraint as useBoxProjectedEnv — needs a new TSL implementation rather than a port."
  },

  "Text": {
    "classification": "todo",
    "reason": "Depended on a vendored troika fork that could not be published. Returns via @pmndrs/glyph, which covers both renderers. Tracked in #2658."
  }
}
